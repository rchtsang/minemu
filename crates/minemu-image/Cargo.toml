[package]
name = "minemu-image"
version = "0.1.0"
edition = "2024"

[dependencies]
minemu-platform = { path = "../minemu-platform" }
object = { version = "0.39.1", default-features = false, features = ["elf", "read"] }
thiserror = "2"
